<IfModule mod_rewrite.c>
    RewriteEngine On

    # Redirect all requests to the public/ directory
    # This is for shared hosting where document root cannot be changed
    RewriteCond %{REQUEST_URI} !^/public/
    RewriteRule ^(.*)$ /public/$1 [L,QSA]
</IfModule>
